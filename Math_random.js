let random=Math.random()+2;//this is use to  generate the random value according to the range with the decimal numve like 0.33,.23etc
console.log(random);
let nGenerate=Math.floor(Math.random()*5+4);//this is use to  generate the random value according to the range  without the decimal like a2,4,5,6 etc
console.log(nGenerate);